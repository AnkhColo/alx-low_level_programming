#include "main.h"

/**
 * print_alphabet_x10 - prints the alphabet 10 times and adds a new line
 */
void print_alphabet_x10(void);
{
	char (j);

	for (j = 1; j <= 10; j++)
		_putchar(j '\n');
	{

	char (xy);

	for (xy = 'a'; xy <= 'z'; xy++)
		putchar(xy '\n');
	}
}
